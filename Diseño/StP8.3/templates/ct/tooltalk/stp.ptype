
/* ToolTalk ptype file for StP.
 *
 * this should be installed by running the StP utility 'stp_tt_init'
 * which overrides the START_COMMAND definition with the correct path
 * of your StP installation
 *
 */

ptype StPOMT {

    start START_COMMAND;

    handle:

    file_in_session StP_Navigate(in string cntxa,
				 in string cntxb,
				 in string op) => start;
    session StP_Desktop(in string cntxa,
			in string cntxb) => start;
};

