//
//      SCE SubprogramCall link
//

// Access Mode
 
Include "annotation/se_generic_object"

Note 802039
{
    { AppType GlobalUsage }
    { Bounds { Min 0 } { Max 1 } }
    { PrintName "Global Usage" }
    { Item
        { AppType AccessMode }
        { PrintName "Access Mode" }
        { HelpText "The Access Mode item lets you specify the access mode for this usage of a piece of global data. Possibilities are read | write | read-write." }
        { Bounds { Min 1 } { Max 1 } }
    }
}

//  Add any user customizations to the file included below:
IncludeIfExists "user/ct/annotation/user_subprogram_call"
