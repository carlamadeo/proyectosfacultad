//	DSE iteration


Include "annotation/data_definition.note"
Include "annotation/structure_component.note"
Include "annotation/generic_object"

// Add these iteration items
Note 802006
{
    { Item
        { AppType LowerIteration }
	{ Bounds { Min 0 } { Max 1 } }
	{ PrintName "Lower Bound" }
        //{ IsReadOnly True }
	{ HelpText "Use the Lower Bound item to indicate the minimum number of these objects in this data structure." }
    }
    { Item
        { AppType UpperIteration }
	{ Bounds { Min 0 } { Max 1 } }
	{ PrintName "Upper Bound" }
        //{ IsReadOnly True }
	{ HelpText "Use the Upper Bound item to indicate the maximum number of these objects that are allowed in this data structure." }
    }
}
Include "annotation/t_description.note"


//  Add any user customizations to the file included below:
IncludeIfExists "user/ct/annotation/user_iteration"
