/**********************************************
 *
 * Utility-Functions for DeploymentDescriptors
 *
 * Author:  Frank Lippert
 * Company: Aonix GmbH
 * Date:    12/05/2000
 **********************************************/
 
proc getResourceAuthentication(MElement)
    loop(MElement->TaggedValue As TG Where [TG.tag] == "ejb_ResAuth") 
        return [TG.value];
    end loop
    // Fallback value
    return "Container";
end proc