
// Annotations added for StP/Validator


Note 810009
{
    { AppType ExpectedOutput }
    { HelpText "An Oracle is a program that generates a simulated expected output" }
    { Bounds { Min 0 } { Max 1 } }
    { PrintName "Expected Output" }
    { Item
        { AppType HasExpectedOutput }
        { HelpText "Has Expected Output" }
        { Bounds { Min 1 } { Max 1 } }
        { PrintName "HasExpectedOutput" }
        { InitValue  "True" }
        { IsReadOnly True }
    }
    { Item
        { AppType ExpectedOutputCommand }
        { HelpText "The name of the Oracle goes here. The Oracle must be placed in the directory ../test_files/ORACLES" }
        { Bounds { Min 0 } { Max 1 } }
        { PrintName "Oracle" }
    }
    { Item
        { AppType OutputArgumentList }
        { HelpText "The list of arguments, separated by commas" }
        { Bounds { Min 0 } { Max 1 } }
        { PrintName "Argument List" }
    }
    { Item
        { AppType OutputHasFixedValue }
        { HelpText "If the Output is fixed, add a description to the Expected Output Note" }
        { Bounds { Min 0 } { Max 1 } }
        { PrintName "Fixed Output" }
        { InitValue  "True" }
        { IsReadOnly True }
    }
}

